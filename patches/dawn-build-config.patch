diff --git a/third_party/dawn/BUILD.gn b/third_party/dawn/BUILD.gn
index 58690d8687..afe994c19e 100644
--- a/third_party/dawn/BUILD.gn
+++ b/third_party/dawn/BUILD.gn
@@ -285,6 +285,32 @@ action("dawn_cmake") {
   } else {
     args += [ "--dawn_enable_vulkan=false" ]
   }
+  if (dawn_use_x11) {
+    args += [ "--dawn_use_x11=true" ]
+  } else {
+    args += [ "--dawn_use_x11=false" ]
+  }
+  if (dawn_use_wayland) {
+    args += [ "--dawn_use_wayland=true" ]
+  } else {
+    args += [ "--dawn_use_wayland=false" ]
+  }
+
+  # Pass build type to CMake (Debug or Release)
+  if (is_debug) {
+    args += [ "--build_type=Debug" ]
+  } else {
+    args += [ "--build_type=Release" ]
+  }
+
+  # Pass MSVC runtime library setting to CMake (Windows only)
+  if (is_win) {
+    if (is_debug) {
+      args += [ "--msvc_runtime_library=MultiThreadedDebug" ]
+    } else {
+      args += [ "--msvc_runtime_library=MultiThreaded" ]
+    }
+  }
 
   args += sanitizer_args
 }
diff --git a/third_party/dawn/args.gni b/third_party/dawn/args.gni
index b2c2e707d1..2fa6619a77 100644
--- a/third_party/dawn/args.gni
+++ b/third_party/dawn/args.gni
@@ -9,4 +9,6 @@ declare_args() {
   dawn_enable_opengles = false
   dawn_enable_metal = is_mac || is_ios
   dawn_enable_vulkan = is_linux || is_android
+  dawn_use_x11 = is_linux
+  dawn_use_wayland = is_linux
 }
diff --git a/third_party/dawn/build_dawn.py b/third_party/dawn/build_dawn.py
index e23889e323..d193cf1476 100644
--- a/third_party/dawn/build_dawn.py
+++ b/third_party/dawn/build_dawn.py
@@ -54,6 +54,12 @@ def main():
       "--dawn_enable_metal", default="false", help="Enable Metal backend.")
   parser.add_argument(
       "--dawn_enable_vulkan", default="false", help="Enable Vulkan backend.")
+  parser.add_argument(
+      "--dawn_use_x11", default="false", help="Enable X11 surface support.")
+  parser.add_argument(
+      "--dawn_use_wayland", default="false", help="Enable Wayland surface support.")
+  parser.add_argument(
+      "--msvc_runtime_library", default="", help="MSVC runtime library (MultiThreaded or MultiThreadedDebug)")
   args = parser.parse_args()
 
   cmake_exe = shutil.which("cmake")
@@ -107,7 +113,10 @@ def main():
       f"-DDAWN_ENABLE_OPENGLES={gn_bool_to_cmake(args.dawn_enable_opengles)}",
       f"-DDAWN_ENABLE_METAL={gn_bool_to_cmake(args.dawn_enable_metal)}",
       f"-DDAWN_ENABLE_VULKAN={gn_bool_to_cmake(args.dawn_enable_vulkan)}",
+      f"-DDAWN_USE_X11={gn_bool_to_cmake(args.dawn_use_x11)}",
+      f"-DDAWN_USE_WAYLAND={gn_bool_to_cmake(args.dawn_use_wayland)}",
   ]
+
   configure_cmd += get_third_party_locations()
 
   if args.enable_rtti:
@@ -122,6 +131,10 @@ def main():
     cxx_flags += win_cxx
     ld_flags += win_ld
 
+    # Override MSVC runtime library if specified (must come after win_cfgs)
+    if args.msvc_runtime_library:
+      configure_cmd.append(f"-DCMAKE_MSVC_RUNTIME_LIBRARY={args.msvc_runtime_library}")
+
     # The D3D backend requires the HLSL writer.
     configure_cmd.append("-DTINT_BUILD_HLSL_WRITER=ON")
   else:
@@ -152,6 +165,11 @@ def main():
 
   if target_os == "Darwin" or target_os == "iOS":
     configure_cmd.append(f"-DCMAKE_OSX_ARCHITECTURES={target_cpu}")
+    if target_os == "iOS":
+      # Get iOS SDK settings from cmake_utils
+      from cmake_utils import get_ios_settings
+      ios_cfgs = get_ios_settings(target_cpu)
+      configure_cmd += ios_cfgs
 
   env = os.environ.copy()
   # Don't write .pyc files, which can cause race conditions when building
diff --git a/third_party/dawn/cmake_utils.py b/third_party/dawn/cmake_utils.py
index f2fd635c18..65c1a8450b 100644
--- a/third_party/dawn/cmake_utils.py
+++ b/third_party/dawn/cmake_utils.py
@@ -234,6 +234,13 @@ def get_cmake_os_cpu(os, cpu):
     }
     return "Darwin", target_cpu_map[cpu]
 
+  if os == "ios":
+    target_cpu_map = {
+      "arm64": "arm64",
+      "x64": "x86_64",
+    }
+    return "iOS", target_cpu_map[cpu]
+
   if os == "win":
     target_cpu_map = {
       "arm64": "ARM64",
@@ -329,6 +336,48 @@ def get_windows_settings(args):
   return win_cfgs, win_cxx, win_ld
 
 
+def get_ios_settings(target_cpu):
+  """Get CMake settings for iOS cross-compilation.
+     Uses xcrun to find the appropriate iOS SDK."""
+  ios_cfgs = []
+
+  # Determine if we're building for device or simulator based on CPU
+  if target_cpu == "arm64":
+    sdk_name = "iphoneos"
+  else:
+    sdk_name = "iphonesimulator"
+
+  # Get SDK path using xcrun
+  try:
+    sdk_path = subprocess.check_output(
+        ["xcrun", "--sdk", sdk_name, "--show-sdk-path"],
+        text=True
+    ).strip()
+  except subprocess.CalledProcessError:
+    print(f"Error: Could not find iOS SDK for {sdk_name}")
+    sys.exit(1)
+
+  ios_cfgs.append(f"-DCMAKE_OSX_SYSROOT={sdk_path}")
+  # Dawn uses C++ atomic wait/notify_all which requires iOS 14.0+
+  ios_cfgs.append("-DCMAKE_OSX_DEPLOYMENT_TARGET=14.0")
+  # Disable bitcode (deprecated in Xcode 14+)
+  ios_cfgs.append("-DCMAKE_XCODE_ATTRIBUTE_ENABLE_BITCODE=NO")
+
+  # Cross-compilation hints for pthreads (built-in on iOS)
+  # CMake's FindThreads module can't run test programs when cross-compiling,
+  # so we need to provide these hints.
+  ios_cfgs.append("-DCMAKE_CROSSCOMPILING=YES")
+  ios_cfgs.append("-DTHREADS_PREFER_PTHREAD_FLAG=ON")
+  ios_cfgs.append("-DCMAKE_THREAD_LIBS_INIT=")  # pthreads built-in
+  ios_cfgs.append("-DCMAKE_HAVE_THREADS_LIBRARY=TRUE")
+  ios_cfgs.append("-DCMAKE_USE_PTHREADS_INIT=TRUE")
+
+  # Disable tint command-line tools for iOS (they require MACOSX_BUNDLE config)
+  ios_cfgs.append("-DTINT_BUILD_CMD_TOOLS=OFF")
+
+  return ios_cfgs
+
+
 def combine_into_library(args, output_path, build_dir, target_os, object_files):
   """Combine all the object files into a single .a/.lib file so it's easier to
      give this to GN."""
@@ -406,7 +455,7 @@ def get_third_party_locations():
     "-DTINT_BUILD_BENCHMARKS=OFF",
     "-DTINT_BUILD_IR_BINARY=OFF",
     "-DTINT_BUILD_TESTS=OFF",
-    "-DDAWN_USE_X11=OFF",
+    # Note: DAWN_USE_X11 is now passed from build_dawn.py based on dawn_use_x11 GN arg
 
     # Explicitly mark third_party deps as not here to make debugging easier
     "-DDAWN_EMDAWNWEBGPU_DIR=NOT_SYNCED_BY_SKIA",
